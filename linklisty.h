


void inity (assem * asmb, unsigned int id);

void addy (assem * asmb, linklist * lst);



void freey (assem * asmb);

void freeally (assem * asmb);

unsigned int numelementy (assem * asmb);

linklist *findbyidy (assem * asmb, unsigned int id);

int replacey (assem * asmb, linklist * lst, unsigned int id);

int deletey (assem * asmb, unsigned int id);

int inserty (assem * asmb, linklist * lst, unsigned int id);

